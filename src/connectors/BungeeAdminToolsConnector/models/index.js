const ban = require('./ban');
const kick = require('./kick');
const mute = require('./mute');
const player = require('./player');
const warn = require('./warn');

module.exports = {
  ban, kick, mute, player, warn,
};
